<!DOCTYPE html>
<html lang="en">
    <head>
        <% include ../partials/head %>
    </head>
    <body class="container">

    <header>
        
    </header>
    <main>
        <H1>Log In</H1>
        <div class="jumbotron">
            <form action="/signup" align="right">
                <button>Sign up</button>
            </form>
            <br/>
            <center>
                <% if (locals.message) { %>
                    <h2><%= locals.message %></h2>
                <% } %>
                <form action="/login" method="POST">
                    <input placeholder="Username" name="username" value=<%= locals.name %>><br/>
                    <input placeholder="Password" type="password" name="password"><br/>
                    <button>Log In</button>
                </form>
            </center>
        </div>
    </main>

</body>
</html>